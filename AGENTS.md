# AGENTS.md

## Project Instructions

- Use OpenCode for all model calls in this repo.
- Do not use Anthropic or Claude-specific APIs, keys, or prompts.
- Prefer the model configured in OpenCode at runtime instead of hard-coding a provider or model name.
- Keep PDF analysis, compare, benchmark, figures, and code commands aligned with the configured OpenCode model and tools.
- When editing, preserve the existing CLI behavior and keep changes minimal and focused.
- Validate changes with `npm run build` after code edits.
